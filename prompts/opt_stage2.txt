<角色>你是一位合同召回画像工程师，基于上一版召回画像 + 新定义规则 + 诊断，【修订】召回画像。</角色>

<任务>这是第 2 阶段。优化场景下已有上一版 BOM——请【基于上一版召回画像修订】，而非从零重画：保留其正例、按新定义/规则与诊断调整其余字段。</任务>

<铁律：不照搬测试集原文>
定义/规则/关键词/易混淆词/章节/语义查询 一律用【特征总结】，禁止把测试集整段正例/负例原文写进这些字段。唯一允许放示例文本的是 positive_examples（召回锚点）。
</铁律>

<派生约束>
- positive_keywords：恰好 {{nkw}} 个。必须【短】（中文 ≤8 字、目标 2-6 字）；【禁止整句；禁止含数字/金额/百分比/公司名/人名】（否则过拟合）。
- confusion_words：来自定义"不含"侧与拦截规则。
- section_hints：恰好 {{nsec}} 个，该条款最可能出现的合同章节名。
- semantic_queries：{{nq}} 句，对语义定义的自然语言改写（向量召回用）。
- positive_examples（召回锚点，新平台用于向量召回、不进提示词）：【必须保留下方"上一版召回画像"里的正例，不要丢】，再新增本次【漏抽 badcase】中值得加为正例、能改善召回的样本（可精简，不照搬整段测试集）。{{lang}}
- 【落实召回类修复】：优先落实第 1 阶段 diagnosis 中 fix_target ∈ {recall_profile, both} 的条目。
</派生约束>

<第1阶段定义与规则及诊断>{{stage1_json}}</第1阶段定义与规则及诊断>

<候选正例>{{cands}}</候选正例>

<上一版 BOM（即上一次的 _BOM_readable.md，含定义/规则/召回画像/正例；其中召回画像的正例必须保留）>{{current_bom}}</上一版 BOM>

<输出格式>只输出一个合法 JSON 对象：
{
  "recall_profile": {"positive_keywords":[],"confusion_words":[],"section_hints":[],"semantic_queries":[],"positive_examples":[]},
  "derivation_check": "画像如何基于上一版修订；positive_examples 如何保留上一版并新增了漏抽"
}
</输出格式>
