# 技术设计文档 · 生成场景后端专项

> 2026-06-29 v2.0
> **当前阶段：只做"生成"场景的后端，优化场景不在此文档范围内。**
> 前端确定 Vue3（本文档不含前端，前端等后端接口验证通过后再开发）。

---

## 1. 生成场景总览

### 1.1 一句话

用户导入合同测试集 Excel → 数据清洗（分类、去重、质量分析）→ 基于清洗后的数据生成语义 BOM（定义 + 抽取规则 + 召回画像）→ 组装成旧平台可用的完整提示词。

### 1.2 数据流（两个模块的交接点）

```
用户上传 Excel
     │
     ▼
┌─────────────────────────────────┐
│  A 模块（杨力）数据预处理          │
│  ├─ 解析 Excel（中英列名自适应）    │
│  ├─ 按条款分组                     │
│  ├─ 精确去重 + 近义去重             │
│  ├─ 数据质量分析（不均衡/空值/格式） │
│  └─ 输出：CleanedTestSet           │
└──────────────┬──────────────────┘
               │  交接契约：CleanedTestSet
               ▼
┌─────────────────────────────────┐
│  B 模块（林大宇）核心生成           │
│  ├─ B1 召回画像（关键词/混淆词等）   │  ← 部分用统计/NLP，部分用大模型
│  ├─ B2 语义定义 + 抽取规则          │  ← 大模型
│  ├─ B3 规则自检                     │  ← 大模型
│  └─ B4 完整提示词组装               │  ← 模板拼装（不用大模型）
│  输出：BOM + FullPrompt             │
└──────────────────────────────────┘
```

### 1.3 两人分工边界

| 模块 | 负责人 | 输入 | 交付物 | 交付契约 |
|---|---|---|---|---|
| **A 数据预处理** | 杨力 | Excel/CSV 文件 | 清洗后的测试集 + 质量报告 | `CleanedTestSet` |
| **B 核心生成** | 林大宇 | `CleanedTestSet` | BOM + 完整提示词 | `BOM` + `FullPrompt` |

**关键：杨力交付 `CleanedTestSet` 后，林大宇才开始工作。两人通过这个契约对接，互不读对方实现内部。**

---

## 2. 模块与文件映射表

> 任何人看到这张表，就能明白哪个文件干什么、谁写、输入输出是什么。

### A 模块（杨力）· 数据预处理

| 文件路径 | 中文名 | 功能说明 | 输入 | 输出 |
|---|---|---|---|---|
| `backend/src/cc_bom_generator/nodes/ingest.py` | 数据加载 | 读 Excel/CSV，中英列名自适应，按语义块编码分组 | 文件路径 | `TestSet`（原始） |
| `backend/src/cc_bom_generator/nodes/dedup.py` | 去重引擎 | 精确去重 + 近义去重（difflib/embedding），打印去掉哪些 | `TestSet` | `TestSet`（去重后） |
| `backend/src/cc_bom_generator/nodes/quality.py` | 质量分析 | 检测不均衡/空值率/格式分布/异常值，生成报告 | `TestSet` | `QualityReport` |
| `backend/src/cc_bom_generator/nodes/clean.py` | 清洗编排 | 把上面三步串起来，输出最终 `CleanedTestSet` | 文件路径 | `CleanedTestSet` |

### B 模块（林大宇）· 核心生成

| 文件路径 | 中文名 | 功能说明 | 输入 | 输出 |
|---|---|---|---|---|
| `backend/src/cc_bom_generator/nodes/keyword_extract.py` | 关键词抽取 | 从期望值中用统计/NLP 抽通用关键词（不调大模型） | `CleanedTestSet` | `List[str]` 候选关键词 |
| `backend/src/cc_bom_generator/nodes/profile_build.py` | 画像生成 | 组装召回画像：关键词+混淆词+章节+语义查询+正例 | `CleanedTestSet` + 候选关键词 | `RecallProfile` |
| `backend/src/cc_bom_generator/nodes/generate.py` | 定义+规则生成 | 调大模型生成语义定义和抽取规则（Stage1） | `CleanedTestSet` + `RecallProfile` | `BOM`（定义+规则部分） |
| `backend/src/cc_bom_generator/nodes/verify.py` | 规则自检 | 调大模型验证正例能抽/反例能拦（Stage3） | `BOM` + 正例 + 反例 | `Verification` |
| `backend/src/cc_bom_generator/nodes/prompt_assemble.py` | 提示词组装 | 基于 BOM+画像+示例组装旧平台可用的完整提示词 | `BOM` + `RecallProfile` | `FullPrompt` |

### 公共模块（两人共享）

| 文件路径 | 中文名 | 功能说明 | 谁写 |
|---|---|---|---|
| `backend/src/cc_bom_generator/llm/client.py` | LLM 客户端 | 调大模型、JSON 解析重试、温度配置 | 杨力先写（B 模块依赖） |
| `backend/src/cc_bom_generator/contracts/*.py` | 契约定义 | 所有输入输出的 Pydantic 模型 | 林大宇（已写完） |
| `backend/src/cc_bom_generator/db/models.py` | 数据库模型 | SQLAlchemy ORM 表定义 | 林大宇 |
| `backend/src/cc_bom_generator/api/routes.py` | API 路由 | FastAPI 接口定义（POST /generate 等） | 林大宇 |

---

## 3. 契约定义（已有 + 新增）

### 3.1 已有契约（已写完，位于 `contracts/`）

| 契约 | 文件 | 中文说明 |
|---|---|---|
| `BOM` | `bom.py` | 语义 BOM 完整定义（条款名+版本+定义+规则+画像） |
| `ExtractionRules` | `bom.py` | 抽取规则（绝对拦截 + 核心匹配） |
| `RecallProfile` | `bom.py` | 召回画像（关键词+混淆词+章节+语义查询+正例） |
| `TestSet` | `test_set.py` | 测试集（文档→语义块→语义项） |
| `Document/Block/Item` | `test_set.py` | 文档/语义块/语义项的层级结构 |

### 3.2 新增契约（需补充到 `contracts/`）

```python
# contracts/cleaned_test_set.py —— A 模块交付给 B 模块的契约

class CleanedTestSet(BaseModel):
    """A 模块清洗后交付给 B 模块的测试集。"""
    clause: str                          # 条款名称
    block_code: str                      # 语义块编码
    domain: str                          # 业务域（采购/销售/服务/工程/框架）

    positive_values: List[str]           # 去重后的期望值列表（正例，已清洗）
    positive_contexts: List[str]         # 对应的上下文原文（如果有的话）

    quality_report: QualityReport        # 数据质量报告

class QualityReport(BaseModel):
    """数据质量分析报告。"""
    original_count: int                  # 原始标注数
    after_exact_dedup: int              # 精确去重后
    after_near_dedup: int               # 近义去重后
    empty_rate: float                    # 空值率
    format_distribution: dict            # 格式分布 {"数值": 0.7, "文本": 0.3}
    balance_warning: List[str]           # 不均衡告警列表
    outlier_count: int                   # 异常值数量
```

```python
# contracts/full_prompt.py —— B 模块最终交付物之一

class FullPrompt(BaseModel):
    """组装好的完整提示词，可直接粘贴到旧平台使用。"""
    clause: str                          # 条款名称
    prompt_text: str                     # 完整提示词文本
    bom_snapshot: BOM                    # 对应的 BOM 快照（可追溯）
    assembled_from: List[str]            # 用了哪些组件组装（["定义","规则","画像","正例"]）
```

---

## 4. 算法与实现建议（重点）

### 4.1 总原则：能不用大模型就不用

大模型用于"需要语义理解和归纳"的环节（语义定义、抽取规则）。能用统计和 NLP 搞定的（关键词、混淆词、格式模板），**用传统方法更快、更稳定、更可解释**。

### 4.2 每个环节的方法选型

| 环节 | 方法 | 是否用大模型 | 理由 |
|---|---|---|---|
| **正向关键词** | TF-IDF + 词频统计 + 规则过滤 | ❌ 不用 | 关键词是高频通用词，统计就够 |
| **易混淆词** | 基于关键词的对比排除 + 领域词典 | ❌ 不用 | 与正向词互斥的词 |
| **章节提示** | 从测试集的 sectionPath 字段统计 | ❌ 不用 | 如果有章节信息直接统计 |
| **语义查询句** | 基于定义改写（模板化） | ⚠️ 轻量调 | 拿定义做模板变换 |
| **语义定义** | 大模型归纳 | ✅ 用 | 需要语义理解和泛化 |
| **抽取规则** | 大模型生成 | ✅ 用 | 需要逻辑推理 |
| **正例挑选** | 聚类 + 中心点选择 | ❌ 不用 | 多样性挑选用聚类 |
| **提示词组装** | 字符串模板拼接 | ❌ 不用 | 纯拼装 |

### 4.3 关键词抽取的具体算法（B 模块核心，不用大模型）

这是你问的重点。分三步：

#### 第一步：分词 + 词频统计

```python
import jieba
from collections import Counter

def extract_candidate_keywords(values: List[str], top_n: int = 30) -> List[tuple]:
    """
    从期望值列表中抽取候选关键词。
    返回 [(词, 得分), ...]
    """
    # 1. jieba 分词（合同文本用 custom_dict 增强领域词）
    all_words = []
    for val in values:
        words = jieba.lcut(val)
        all_words.extend(words)

    # 2. 过滤停用词 + 标点 + 单字
    filtered = [w for w in all_words
                if len(w) >= 2
                and w not in STOPWORDS
                and not w.isdigit()
                and not all(c in '，。、：；！？（）()[]【】' for c in w)]

    # 3. 词频统计
    freq = Counter(filtered)

    # 4. TF-IDF 加权（如果有多个条款的语料做 IDF）
    # 简化版：直接用词频 / 总词数 作为 TF
    total = sum(freq.values())
    scored = [(word, count / total) for word, count in freq.most_common(top_n)]
    return scored
```

#### 第二步：规则过滤（防过拟合）

```python
def filter_keywords(candidates: List[tuple]) -> List[str]:
    """
    过滤掉不适合做精确匹配的候选词。
    """
    result = []
    for word, score in candidates:
        # 规则1：长度 2-8 字（中文）
        if len(word) < 2 or len(word) > 8:
            continue
        # 规则2：不含数字/金额/百分比（防过拟合）
        if any(c.isdigit() or c in '%％万元亿' for c in word):
            continue
        # 规则3：不含公司名/人名（用 NER 或正则）
        if re.search(r'[有限公司股份集团]', word):
            continue
        # 规则4：频次 ≥ 2（至少出现两次才算通用）
        if score < 2 / len(candidates):
            continue
        result.append(word)
    return result[:10]  # 取 top 10
```

#### 第三步：可选升级（小模型 / TF-IDF 加权）

如果词频统计不够好（比如"合同"这种通用词排第一），升级方案：

| 升级方案 | 方法 | 适用场景 | 复杂度 |
|---|---|---|---|
| **TF-IDF** | 把所有条款的期望值当文档集，算 IDF 权重 | "合同"这种通用词 IDF 低，自动降权 | 低（sklearn 一行） |
| **TextRank** | 基于词共现图做 PageRank 排序 | 抽出的词更"关键" | 中（jieba 自带） |
| **KeyBERT** | 用 BERT embedding 做相似度排序 | 抽出的词语义更准 | 中（需装 sentence-transformers） |
| **领域词典** | 预定义合同高频术语表，与统计结果取交集 | 精确、可控 | 低 |

**我的建议**：先用 jieba + 词频 + 规则过滤（零依赖、快、可解释），跑一轮看效果。如果关键词质量不够（比如出现太多"甲乙双方"这种噪声），再上 TF-IDF。**不建议一开始就上 BERT/KeyBERT，那是 Phase 2 的事。**

### 4.4 易混淆词的抽取方法

```python
def extract_confusion_words(keywords: List[str], all_values: List[str]) -> List[str]:
    """
    从期望值中找"与关键词相近但不是关键词"的词。
    方法：分词后，找那些与关键词字面相似（编辑距离≤2）
    但本身不在关键词列表里的高频词。
    """
    confusion = []
    kw_set = set(keywords)
    all_words = [w for val in all_values for w in jieba.lcut(val) if len(w) >= 2]
    freq = Counter(all_words)

    for word, count in freq.most_common(100):
        if word in kw_set:
            continue
        # 与任一关键词编辑距离 ≤ 2 → 可能是混淆词
        for kw in keywords:
            if levenshtein(word, kw) <= 2:
                confusion.append(word)
                break
    return confusion[:6]
```

### 4.5 正例挑选（多样性聚类）

```python
from sklearn.cluster import KMeans
from sklearn.feature_extraction.text import TfidfVectorizer

def select_diverse_examples(values: List[str], n: int = 5) -> List[str]:
    """
    从期望值列表中选 n 个互相差异最大的代表性样本。
    方法：TF-IDF 向量化 → KMeans 聚类 → 每类取离中心最近的样本。
    """
    if len(values) <= n:
        return values

    vectorizer = TfidfVectorizer(tokenizer=jieba.lcut)
    X = vectorizer.fit_transform(values)
    k = min(n, len(values))
    km = KMeans(n_clusters=k, random_state=42, n_init=10)
    km.fit(X)

    selected = []
    for i in range(k):
        cluster_indices = [j for j, label in enumerate(km.labels_) if label == i]
        if not cluster_indices:
            continue
        # 取离中心最近的
        distances = km.transform(X[cluster_indices])[:, i]
        nearest = cluster_indices[distances.argmin()]
        selected.append(values[nearest])
    return selected[:n]
```

### 4.6 章节提示

如果测试集 Excel 里有 `sectionPath` 或章节相关字段：

```python
def extract_section_hints(testset, top_n=6):
    """直接从数据中统计章节名，不需要大模型。"""
    sections = [doc.section for doc in testset.documents if doc.section]
    freq = Counter(sections)
    return [s for s, _ in freq.most_common(top_n)]
```

如果没有章节字段（大多数情况），则这一步交给大模型在 Stage1 里推断（定义生成时附带章节提示）。

### 4.7 语义定义 + 抽取规则（必须用大模型）

这两步没有可靠的统计替代方案，必须用大模型的归纳推理能力：

- **输入**：条款名 + 去重后的期望值 + 统计出的关键词列表（辅助参考）
- **输出**：自然语言定义 + 抽取规则
- **提示词**：`prompts/gen_stage1.txt`（已有，复用）
- **温度**：0.2（收敛，规则要稳定）

### 4.8 完整提示词组装（不用大模型）

```python
def assemble_full_prompt(bom: BOM) -> FullPrompt:
    """
    基于 BOM + 画像组装旧平台可用的完整提示词。
    纯模板拼装，不调大模型。
    """
    template = """你是一个专业的合同条款抽取助手。请从以下合同原文窗口中抽取语义块：{clause}（{block_code}）。

目标定义：{definition}

【抽取规则】
{rules}

【目标画像】
{profile}

--- 合同原文窗口 ---
（用户在此粘贴合同原文）

--- 输出格式 ---
请以 JSON 格式返回抽取结果。"""

    rules_text = format_rules(bom.extraction_rules)
    profile_text = format_profile(bom.recall_profile)

    prompt = template.format(
        clause=bom.clause,
        block_code=bom.block_code,
        definition=bom.semantic_definition,
        rules=rules_text,
        profile=profile_text,
    )
    return FullPrompt(clause=bom.clause, prompt_text=prompt, bom_snapshot=bom,
                      assembled_from=["定义", "规则", "画像"])
```

---

## 5. B 模块的完整执行顺序

```
输入：CleanedTestSet（来自 A 模块）
  │
  ├─ B1. keyword_extract.py    关键词抽取（统计+过滤，不用大模型）
  │     └─ 输出：候选关键词列表
  │
  ├─ B2. profile_build.py      画像组装
  │     ├─ 正向关键词 ← 来自 B1
  │     ├─ 易混淆词   ← 来自 B1（对比排除）
  │     ├─ 章节提示   ← 统计 or 大模型
  │     ├─ 语义查询   ← 大模型（基于定义改写）
  │     └─ 正例       ← 聚类挑选（不用大模型）
  │
  ├─ B3. generate.py           定义+规则（大模型 Stage1）
  │     └─ 输出：BOM 的 definition + extraction_rules
  │
  ├─ B4. verify.py             规则自检（大模型 Stage3）
  │     └─ 输出：Verification（红旗/覆盖估计）
  │
  └─ B5. prompt_assemble.py    提示词组装（模板拼装，不用大模型）
        └─ 输出：FullPrompt（可直接粘贴旧平台）
```

**注意执行顺序**：B3（定义）必须先于 B2 的语义查询部分（语义查询基于定义改写）。所以实际编排是：

```
B1 关键词 → B3 定义+规则 → B2 画像（关键词+混淆词+章节+语义查询+正例）→ B4 自检 → B5 组装
```

---

## 6. 需要安装的 Python 依赖

```python
# backend/requirements.txt（A+B 模块合计）
# --- A 模块（杨力）---
pandas>=2.0            # Excel/CSV 读取
openpyxl>=3.1          # xlsx 引擎
difflib                # 近义去重（标准库，不用装）

# --- B 模块（林大宇）---
jieba>=0.42            # 中文分词
scikit-learn>=1.3      # TF-IDF + KMeans 聚类
openai>=1.0            # 大模型调用

# --- 公共 ---
pydantic>=2.0          # 契约
fastapi>=0.104          # API 框架
uvicorn>=0.24          # ASGI 服务器
sqlalchemy>=2.0        # ORM
alembic>=1.12          # 迁移
pymysql>=1.0           # MySQL 驱动
pyyaml>=6.0            # 配置读取
```

---

## 7. 开发顺序（第一周）

| 天 | 杨力（A 模块） | 林大宇（B 模块） |
|---|---|---|
| Day 1 | `ingest.py`（Excel 解析+分组） | `llm/client.py`（等不了杨力，先写大模型客户端） |
| Day 2 | `dedup.py`（去重+打印明细） | `keyword_extract.py`（jieba+词频+过滤） |
| Day 3 | `quality.py`（质量分析报告） | `generate.py`（定义+规则，调大模型） |
| Day 4 | `clean.py`（编排→输出 CleanedTestSet） | `profile_build.py`（画像组装） |
| Day 5 | 联调：用真实 Excel 跑通 A 模块 | 联调：接 A 模块输出，跑通 B1-B3 |
```
