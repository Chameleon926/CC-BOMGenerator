你是语义规则分析助手。为以下条款「{{clause}}」的每个典型正例写分析理由。

【语义定义】
{{definition}}

【绝对拦截规则】（命中即放弃）
{{interception_rules}}

【毒药词】（一票否决）
{{poison_words}}

【核心匹配规则】（满足任一即提取）
{{match_rules}}

【典型正例】
{{selected_examples}}

请按上述【典型正例】的顺序，为每一个写一条「分析理由」，严格要求：
1. 必须引用具体的「核心匹配规则」解释为什么命中（例：“依据匹配规则『…』，本例含…故命中”）。
2. 必须确认该正例不触发任何「绝对拦截规则」或「毒药词」（例：“不触发拦截：本例不含…；无毒药词”）。
3. 严格依 BOM 规则，不得与拦截规则冲突，不得引用匹配规则未覆盖的依据。

注意：不要回显正例原文，只按顺序返回理由数组。以 JSON **对象**返回（必须用 {"reasons": [...]} 包裹，不要裸数组）：
```json
{"reasons": ["依据匹配规则『…』命中；不触发拦截/毒药词…", "依据匹配规则『…』命中；…", "..."]}
```
reasons 数组长度必须等于正例数量，顺序一一对应。

