# 大模型配置 —— 复制本文件为 llm.yaml 并填写你的值：
#   cp config/llm.example.yaml config/llm.yaml
# llm.yaml 已被 .gitignore 忽略（绝不入库）；每位开发者各自填写，互不可见。

# API Key（必填）
api_key: ""

# 接口地址（OpenAI 兼容）。留空 = 用 SDK 默认端点；
# 接 Gemini / 自建 / 代理则填对应地址，例：
#   https://generativelanguage.googleapis.com/v1beta/openai
base_url: ""

# 模型名（必填），例：gpt-4o-mini / gemini-2.5-flash / 你自建模型名
model: ""

# 采样温度（可选，默认 0.3）
temperature: 0.3

# === 内网代理/SSL（内网部署用，外网留空即可）===

# HTTP 代理：内网如有代理填这里，例 "http://proxy.corp.com:8080"；不需要留空
proxy: ""

# SSL 证书验证：
#   true  = 默认验证（外网用）
#   false = 跳过验证（内网自签证书，测试用）
#   "/path/to/ca-bundle.crt" = 指定内网 CA 证书路径
ssl_verify: true
